var startButtonEl = $('#start-btn');


startButtonEl.on('click', function () {
    starttimer();
})

function setTime() {
    var timerInterval = setInterval(function() {
        secondsLeft--;
        timeEl
    })
}